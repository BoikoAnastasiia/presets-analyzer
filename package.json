{
  "name": "preset-analyzer",
  "version": "1.0.0",
  "description": "Analyze preset JSON files and export to CSV",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}
